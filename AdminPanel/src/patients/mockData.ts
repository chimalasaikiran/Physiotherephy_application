import type { Patient } from './types';

// Deprecated mock data file - Patients data is now dynamically retrieved from Firestore 'patient details' collection via Patients Service
export const INITIAL_PATIENTS: Patient[] = [];
